name: CI

on:
  push:
    branches: [main]
  pull_request:

# Un solo trabajo en vuelo por rama: empujar dos veces seguidas cancela el
# anterior en vez de dejar dos compilaciones compitiendo.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verificar:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    env:
      # Un valor de mentira: la compilación necesita que EXISTA, no que sirva.
      # Las páginas se compilan sin tocar la red;
      # las consultas reales ocurren en cada petición, ya en producción.
      SITE_URL: https://ejemplo.test

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Instalar
        run: npm ci

      - name: Formato
        run: npm run format:check

      - name: Tipos
        run: npm run check

      - name: Pruebas
        run: npm test

      - name: Compilar
        run: npm run build
